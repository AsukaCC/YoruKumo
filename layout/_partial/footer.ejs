<footer class="footer-container">
  <div class="footer-content">
    <p class="footer-text">
      <%= __('website.footer.reserved') %> @
      <a href="<%= theme.footer.authorLink %>" target="_blank"
        ><%= __('website.footer.author') %></a
      >
    </p>
    <p class="footer-text">
      <%= __('website.footer.powered') %>
      <span id="runtime"></span>
    </p>
  </div>
</footer>

<script>
  function calculateRuntime() {
    // 网站开始运行的时间，格式：年-月-日
    const startTime = new Date('<%= theme.footer.since %>');
    const currentTime = new Date();

    const timeDiff = currentTime - startTime;
    const days = Math.floor(timeDiff / (1000 * 60 * 60 * 24));
    const hours = Math.floor(
      (timeDiff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
    );
    const minutes = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((timeDiff % (1000 * 60)) / 1000);

    const dayStr = '<%= __("website.footer.day") %>';
    const hourStr = '<%= __("website.footer.hour") %>';
    const minuteStr = '<%= __("website.footer.minute") %>';
    const secondStr = '<%= __("website.footer.second") %>';

    document.getElementById(
      'runtime'
    ).innerHTML = `${days}${dayStr}${hours}${hourStr}${minutes}${minuteStr}${seconds}${secondStr}`;
  }

  // 初始运行
  calculateRuntime();
  // 每秒更新一次
  setInterval(calculateRuntime, 1000);
</script>
